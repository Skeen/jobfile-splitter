{
  "name": "jobfile-splitter",
  "version": "0.0.1",
  "description": "Split a job-file into two using a specified technique",
  "main": "index.js",
  "scripts": {
    "predist": "mkdir -p dist; npm install; npm install browserify",
    "dist": "./node_modules/browserify/bin/cmd.js -s knn_confusion --bare index.js -o dist/knn_confusion.js",
    "clean": "rm -rf dist node_modules",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "commander": "^2.9.0",
    "readline": "^1.3.0"
  }
}
